<p>こんにちは <%= @resource.email %>さん!</p>

<p>パスワード変更がリクエストされました。以下のリンクから変更が可能です。</p>

<p><%= link_to 'パスワード変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>リクエストしていない場合は、このメールを無視してください。</p>
<p>上のリンクにアクセスして新しいパスワードを作成するまで、パスワードは変更されません。</p>
